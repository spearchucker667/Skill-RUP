name: Release Package

on:
  release:
    types: [created]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: write

    env:
      TAG_NAME: ${{ github.ref_name }}
      RUP_UPSTREAM_DIR: ${{ github.workspace }}/.reference/rup-protocol-ci

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Check out pinned canonical protocol
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: spearchucker667/RUP-Protocol
          ref: c3d6f70375db15d53db2fba76d70b5b7c9cf98bb
          path: .reference/rup-protocol-ci
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-ci.txt

      - name: Validate release source
        run: |
          python -m pytest tests/ -q
          python scripts/generate_schemas_templates.py --check
          python scripts/generate_workflows.py --check
          python scripts/build_capability_map.py --check
          python scripts/audit_sources.py --check --upstream-dir "$RUP_UPSTREAM_DIR"
          python scripts/validate_rup.py --schema protocol/rup-schema.json all .
          git diff --exit-code

      - name: Package skill
        id: package
        shell: bash
        run: |
          TAG_VERSION="${TAG_NAME#v}"
          REPO_VERSION="$(tr -d '[:space:]' < VERSION)"
          if [ "$TAG_VERSION" != "$REPO_VERSION" ]; then
            echo "::error::Release tag ${TAG_NAME} does not match VERSION ${REPO_VERSION}"
            exit 1
          fi
          PACKAGE_NAME="rup-skill-${TAG_NAME}.zip"
          python scripts/package_skill.py \
            --version "${TAG_VERSION}" \
            --output "dist/${PACKAGE_NAME}"
          echo "package_name=${PACKAGE_NAME}" >> "$GITHUB_OUTPUT"

      - name: Verify package
        run: |
          python scripts/package_skill.py \
            --verify \
            --output "dist/${{ steps.package.outputs.package_name }}"

      - name: Validate packaged skill
        run: |
          mkdir -p /tmp/skill-extract
          unzip -q "dist/${{ steps.package.outputs.package_name }}" \
            -d /tmp/skill-extract
          skills-ref validate /tmp/skill-extract/rup

      - name: Upload release assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228
        with:
          files: |
            dist/${{ steps.package.outputs.package_name }}
            dist/${{ steps.package.outputs.package_name }}.sha256
